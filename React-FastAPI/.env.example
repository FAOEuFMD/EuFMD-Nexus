# Database Configuration
# 4 DB names. Ask project manager / teammate for these.
DB_NAME="<db-name>"
DB2_NAME="<db-name>"
DB3_NAME="<db-name>"
DB4_NAME="<db-name>"

# Database Connection
DB_PASS="<your-password>"
DB_HOST="localhost"
DB_USER="<your-username>"
DATABASE_URL="mysql://<your-username>:<your-password>@localhost/<db-name>"

# JWT Secret
SUPER_SECRET="can-be-anything-anything-at-all"
JWT_SECRET="your-jwt-secret-key"
JWT_ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Server Configuration
BACKEND_PORT=8000
FRONTEND_PORT=3000
NODE_ENV="development"

# CORS Configuration
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# AWS Configuration (if needed)
# DB_PASS="<password>"
# DB_HOST="<host>"
# DATABASE_URL="mysql://<username>:<password>@<host>/<db-name>"
# DB_USER="<username>"
